package com.devkuma.tutorial.junit;

public class Calc {
    public int plus(int x, int y) {
        return (x + y);
    }

    protected int minus(int x, int y) {
        return (x - y);
    }

    private int multiply(int x, int y) {
        return (x * y);
    }
}
